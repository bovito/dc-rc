sudo: required
language: node_js
node_js:
  - "7"

services:
  - docker
before_install:
  - npm install
  - docker build -t test -f Dockerfile.dev .
script:
  - docker run -e CI=true test npm run test -- --coverage
