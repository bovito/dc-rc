language: node_js
node_js:
  - "12"
sudo: required
services:
  - docker
before_install:
  - docker build -t bovito/dc-rc -f Dockerfile.dev
script:
  - docker run -e CI=true bovito/dc-rc npm run test -- --coverage
